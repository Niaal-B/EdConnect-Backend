upstream backend_app {
    server backend:8000;
}

server {
    listen 80;
    server_name api.ahamedshamil.in;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        # Temporarily commented out for bootstrap
        # return 301 https://$host$request_uri;
        
        # Simple proxy to backend while we wait for SSL
        proxy_pass http://backend_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

# Temporarily commented out until certificates are obtained
# server {
#     listen 443 ssl;
#     server_name api.ahamedshamil.in;
#     client_max_body_size 10M;
#
#     ssl_certificate /etc/letsencrypt/live/api.ahamedshamil.in/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.ahamedshamil.in/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location /ws/ {
#         proxy_pass http://backend_app;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         proxy_pass http://backend_app/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static/ {
#         alias /app/staticfiles/;
#     }
# }
